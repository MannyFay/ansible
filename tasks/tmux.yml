- name: Link Own tmux Configuration File
  #become: true
  owner: manny
  group: manny
  command: ln -s "{{ lookup('env', 'HOME') }}/dotfiles/tmux/.tmux.conf" "{{ lookup('env', 'HOME') }}/.tmux.conf"
  tags:
    - install
    - core
    - tmux
